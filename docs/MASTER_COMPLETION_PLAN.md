# Vanta Master Completion Plan

Last audited: 2026-07-13

## Audit evidence

| Area                                      | Classification                                       | Evidence                                                                                                                                                                                                                                                                                  | Gate / next action                                                                   |
| ----------------------------------------- | ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| Desktop lifecycle, loopback auth and CORS | verified complete                                    | `apps/desktop/src-tauri/src/lib.rs`, `test_release_runtime.py`, installed-release `orchestrator.log` at `%APPDATA%/studio.vanta.desktop/logs` shows packaged `OPTIONS 200` and authenticated `GET 200`.                                                                                   | Preserve; include in regression checks.                                              |
| No-console desktop and sidecar            | implemented but installed UI verification incomplete | GUI subsystem in `src-tauri/src/main.rs`; sidecar build uses `--noconsole`; Comfy launch uses `CREATE_NO_WINDOW`. NSIS release launched without a tracked console process.                                                                                                                | Complete visual and normal/forced-close acceptance in Phase 0.                       |
| Managed ComfyUI runtime                   | verified complete outside installed UI               | `comfy_runtime.py`, core manifest, bundled `engine/tools/7zr.exe`; verified archive extraction, loopback health and hidden process launch in `build/p0-runtime-4`.                                                                                                                        | Repeat through installed setup UI.                                                   |
| SDXL model import and verification        | verified complete outside installed UI               | `EngineService.import_model` and `verify_model`; imported checkpoint hash and real diagnostic workflow succeeded.                                                                                                                                                                         | Repeat through installed setup UI.                                                   |
| FLUX model-family generation              | verified complete outside installed UI               | The preserved 17,078,891,958-byte self-contained FLUX checkpoint was verified in place (SHA-256 `2eda627c…bb713`) and produced persisted 768×1024 generation `generation-e91bd1d704bf4ebda45d4f44bf1d333d` in 77.20 s through native workflow `image-flux-photoreal-v1` on the 12 GB GPU. | Repeat model selection and generation through the final installed UI.                |
| Image workflow and queue                  | verified complete outside installed UI               | `WorkflowCompiler`, `GenerationService`; real 832x1216 SDXL output, persisted metadata, thumbnail and cancellation evidence in `build/p0-runtime-4`.                                                                                                                                      | Verify installed UI, restart persistence and Generate Similar.                       |
| Gallery media, detail and reproducibility | partially implemented                                | Central authenticated blob loading now uses thumbnail/original endpoints; `repair-media` regenerates missing thumbnails only from real originals. Detail retains reproducibility metadata.                                                                                                | Add native open/copy and more derivative tools as their real packs become available. |
| Persistent job center and progress        | verified complete outside installed UI               | `004_job_progress.sql`, `GenerationService` and the global shell Jobs drawer persist queue state, cancellation/retry and real ComfyUI sampling steps.                                                                                                                                     | Installed UI walkthrough remains in the manual checklist.                            |
| First-run setup                           | implemented but manual verification pending          | `SetupWizard` drives the existing engine install/start, native model picker/import and diagnostic verification; progress derives from persisted component/model state and completion is recorded in `app_settings`.                                                                       | Installed release walkthrough is tracked in `MANUAL_ACCEPTANCE_CHECKLIST.md`.        |
| Characters and preset CRUD                | partially implemented                                | Migration `003_character_references_loras.sql`, repositories, typed routes and authored React cards now preserve expanded profiles, archive/restore/duplicate and local owned-reference storage.                                                                                          | Add drag-and-drop, reference ordering controls and permanent-delete confirmation.    |
| Recipes and prompt compiler               | partially implemented                                | Recipes, preset selection and deterministic compiler exist; Studio controls are limited and recipe persistence lacks full settings.                                                                                                                                                       | Phase 5 after Phase 0–4.                                                             |
| LoRA import/application                   | verified complete outside installed UI               | `lora_packs`/`character_loras`, `LoraRepository`, `LoraLoader` compiler nodes and character import actions. A real 12.9 MB SDXL LoRA (`d7a59b…ecdb`) completed a persisted 832×1216 generation in 19.47 s.                                                                                | Installed UI walkthrough remains in the manual checklist.                            |
| Identity locking                          | intentionally disabled                               | No managed identity dependency with a reviewed source, license, revision, hash and Windows health/repair route is present in this repository.                                                                                                                                             | Keep disabled; do not claim a face-preservation pipeline.                            |
| Pose control                              | intentionally disabled                               | No managed pose preprocessor, ControlNet model pack or reviewed Windows dependency is present in this repository.                                                                                                                                                                         | Keep disabled; do not claim pose extraction or guidance.                             |
| Inpainting, variations, upscaling         | partially implemented                                | The managed SDXL compiler now uses `LoadImage` + `VAEEncode` image-to-image variation. A real local variation completed in 17.62 s with source generation metadata. Inpainting and upscaling are not implemented.                                                                         | Add a managed inpainting/upscaler dependency only after manifest review.             |
| Training and video                        | intentionally deferred                               | No trainer/video adapter, model packs or jobs.                                                                                                                                                                                                                                            | Phases 6–7.                                                                          |
| Diagnostics and installer upgrades        | partially implemented                                | Sanitized zip export, logs and NSIS packaging exist. About screen, broad health actions, upgrade/uninstall coverage remain missing.                                                                                                                                                       | Phases 9–11.                                                                         |

## Phase gates

### Phase 0 — harden real image generation (in progress)

1. Add persistent first-run setup state and a truthful in-app wizard for engine installation, model import, verification and diagnostic generation.
2. Make current readiness, storage and failure actions visible from Create and Models & Engine without raw engine details.
3. Add integration coverage for runtime-install recovery, model verification, Gallery persistence, Generate Similar and cancellation.
4. Build and install NSIS, then run the full installed-release acceptance: setup, import, generation, cancellation, restart persistence, similar request, normal cleanup and forced cleanup.
5. Update stale fixture-era documentation and record the resulting checkpoint.

Noninteractive Phase 0 checks are complete. Native installer, file-picker and close-path checks are tracked in `MANUAL_ACCEPTANCE_CHECKLIST.md`; they do not block independent local feature work. Unsupported capabilities remain disabled and labelled Coming later.

### Later phases — not started

1. Character profiles, reference management and real LoRA import/application.
2. Managed identity locking.
3. Managed pose control and library.
4. Derivative editing, inpainting, variations and upscaling.
5. Complete recipes/presets and Studio controls.
6. Local LoRA training.
7. Optional local video and reference motion.
8. Full component management, diagnostics, polish and upgrade safety.

## Source-control status

The workspace was not a Git repository at audit start. It is now initialized with baseline checkpoint `e7977dc`; build outputs, local runtime tests, imported models and generated media remain ignored.
